while IFS= read -r line; do KEY=`echo $line | awk -F "@" '{print $1}'`; VALUE=`echo $line | awk -F "@" '{print $2}'`; echo "$VALUE" | kaf produce --key $KEY order-testing; done < order.json
while IFS= read -r line; do KEY=`echo $line | awk -F "@" '{print $1}'`; VALUE=`echo $line | awk -F "@" '{print $2}'`; echo "$VALUE" | kaf produce --key $KEY inventory-testing; done < inventory.json
while IFS= read -r line; do KEY=`echo $line | awk -F "@" '{print $1}'`; VALUE=`echo $line | awk -F "@" '{print $2}'`; echo "$VALUE" | kaf produce --key $KEY transportation-testing; done < transportation.json
while IFS= read -r line; do KEY=`echo $line | awk -F "@" '{print $1}'`; VALUE=`echo $line | awk -F "@" '{print $2}'`; echo "$VALUE" | kaf produce --key $KEY reefer-testing; done < reefer.json